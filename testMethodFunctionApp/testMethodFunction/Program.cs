//write a maethod that looks at number and sees if it is less tah 20, if it is it retuns this is less thasn 20
//retuens parementer

using testMethodFunction;

int isNumberBig = Terminal.FunctioningMethod(9);
Console.WriteLine(isNumberBig);